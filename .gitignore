# Annotation pipeline — raw user data and intermediate artifacts must NEVER be committed
messages/raw/
messages/masked/
messages/windows/
messages/labels/
messages/reports/
tools/annotation/data/raw/
tools/annotation/data/masked/
tools/annotation/data/windows/
tools/annotation/data/labels/
tools/annotation/data/reports/

# Extra build artifacts / coverage
build/
coverage/

# All env files (defense-in-depth: .env is already listed below)
.env.*

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment files (contain secrets)
.env
.env.local
.env.*.local

# SSH / TLS keys (must never be committed)
*.pem
*.key

# Let's Encrypt / certbot data
certbot/conf/
certbot/www/

# Pulled production SQLite snapshots (raw user data — never commit)
*.sqlite
*.sqlite-wal
*.sqlite-shm
tools/db-pull/

# Local dev/agent config + content artifacts (not part of product)
.claude/
marketing/
tools/annotation/node_modules/
tools/marketing-screenshots/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
